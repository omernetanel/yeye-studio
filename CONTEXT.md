# CONTEXT — מצב הפרויקט

מסמך העברה לשיחות חדשות. נטען אוטומטית דרך `CLAUDE.md`.
עודכן לאחרונה: 2026-08-07.

---

## מה זה

אתר תדמית ל‑**YEYE Digital** — סטודיו דיגיטלי של איש אחד (עומר).
עברית, RTL, שחור־לבן, טיפוגרפיה גדולה, בלי צבעי מותג.

- **סטאק:** Next.js App Router, TypeScript, Tailwind v4, framer-motion, Lenis (גלילה חלקה), WebGL2 גולמי (סימולציית הדיו).
- **דיפלוי:** Vercel, אוטומטי מכל push ל‑`main`.
- **ריפו:** `omernetanel/yeye-studio`, ענף `main`.

---

## סדר הדף (`app/page.tsx`)

**דסקטופ** (`app/page.tsx`):
`HeroSection` → `ServicesSection` → `StatementSection` → `ContactStage` → `AboutSection` → `ProjectsSection` → `CTASection` → `Footer`

**מובייל** (`components/home/MobileHome.tsx`):
`HeroSection` → `MobileServices` → `ContactStage` → `AboutSection` → `ProjectsSection` → `CTASection` → `Footer`

במובייל אין `StatementSection` — `MobileServices` נושא בעצמו את המשפט הסוגר,
ושניהם יחד היו מציגים אותו פעמיים.

`ProcessSection` **נמחק** (2026-08-07). התוכן שלו — ארבעת שלבי העבודה — מצויר
היום כתרשים בתוך הקליפ של הנייר, ו`AboutSection` תפס את הרקע השחור שלו.

---

## הדפוס המרכזי — הצמדה אנליטית

**כל הסקשנים המפוננים משתמשים באותו דפוס: `position: sticky` + חישוב ידני מ‑`scrollY`.**

לא GSAP ScrollTrigger, ולא `scrollYProgress` של framer-motion. שניהם נוסו והוסרו:
ה‑easing שלהם הצטבר עם הגלילה החלקה של Lenis, כך שגלילה עדינה כמעט לא נרשמה
וגלילה מהירה דילגה על שלבים. מדידת rect חיה + `scrollY` גולמי לא סובלים מזה.

**אל תחליף את זה בספרייה בלי סיבה חזקה מאוד.**

---

## הסקשנים

### HeroSection + FluidInkReveal

מסך פתיחה לבן עם סימולציית דיו ב‑WebGL. הלוגו, ובדסקטופ גם המשפט והכפתורים,
מצוירים על קנבס 2D ("הנייר") שהשיידר חושף/הופך מתחת לדיו.

- **בדסקטופ:** ההיפוך קורה בשיידר, רק בתוך מלבנים מוצהרים (`taglineRect`, `ctaRect`).
  מחוץ להם הדיו פשוט מכפיל ומעלים.
- **במובייל:** ענף נפרד לגמרי. `100svh`, שלושה בלוקים מעוגנים — משפט למעלה,
  לוגו במרכז, כפתור ושורת סטודיו למטה. הלוגו והכפתור חולקים בדיוק את רוחב העמודה.
  **CTA אחד** ("קבעו פגישה") + קישור טקסט ("העבודות שלי").

### ServicesSection — הסקשן הגדול והמורכב ביותר

**דסקטופ בלבד.** במובייל `HomeSwitch` מגיש `MobileServices` במקומו, והענף
המוביילי שהיה כאן נמחק.

הקליפ הוחלף (2026-08-07): `servicesbg.mp4` הוא עכשיו 1920×1080, 30fps,
578 פריימים, **19.267 שניות**, GOP 5, בלי אודיו. **על הדף הפתוח מצויר תרשים
תהליך העבודה** — ארבעה שלבים סביב שרטוט הוורדמארק. מה שהקליפ עושה, נמדד
פריים אחר פריים:

| זמן | מה קורה |
|---|---|
| 0.0–1.3 | כדור במנוחה, רשימת השירותים לצידו |
| 1.3–3.4 | נפתח, התרשים מתגלה |
| 3.4–6.3 | הדף שטוח, התרשים קריא |
| 6.3–7.0 | מתקמט חזרה לכדור |
| 7.0–12.2 | הכדור סתם יושב — אוויר מהעריכה |
| 12.2–13.0 | הופך למטוס |
| 13.0–18.7 | המטוס עף ויוצא |
| 18.7–19.27 | לבן ריק |

**`TIME_HOLDS` הוחלף ב`TIMELINE`** — טבלת נקודות `(progress, time)` עם
אינטרפולציה. שתי נקודות באותו זמן = הקפאה; מרווח progress רחב = קצב רגיל;
צר = מהיר. זה החליף זוג משוואות שידע לבטא הקפאה בלבד.

**הכלל שנקנה כאן ביוקר: מחלקים גלילה לפי כמה משתנה בתמונה, לא לפי זמן בקליפ.**
חלוקה לפי זמן נתנה להתקמטות ולהפיכה למטוס — שתיהן מתחת לשנייה — פחות ממסך
כל אחת, כך שהן עברו בלי להירשם, בעוד שהמטוס שמתרחק (5.7 שניות של כמעט כלום)
קיבל חמישה מסכים ונראה תקוע באוויר. היום שתי הטרנספורמציות מקבלות 200vh
כל אחת, והמעוף 180.

**המסגור:** במנוחה הקליפ ב`scale 1.10` ומוזז 45px — זה מכוון ולא לגעת, הכותרת
והשורות הולחנו סביבו. מרגע שהנייר נפתח הוא **מכסה את המסך** (cover), נפתר חי
מול החלון ולא מספר שכוונן בעין. הגרסה הישנה כיווצה ל‑0.86 בתוך אזור גבוה
מהמסך, והדף עדיין נחתך — מה שהיה נסבל כשהנייר היה טקסטורה ולא כשיש עליו
תרשים לקרוא.

### AboutSection — "מי אני?"

חדש (2026-08-07), על הרקע השחור שהיה של `ProcessSection`. נפתח על השאלה
ואז הופך לכותרת:

1. "רגע, מי אני בעצם?" עולה מתחת לקצה המסך, מוגדל ומטושטש, מתחדד בדרך
2. צף על מסך ריק, גדל מעט, חד לגמרי
3. "רגע," ו"בעצם" **נמחקים** — הגובה של השורה והרוחב של המילה יורדים לאפס,
   כך שסימן השאלה נסגר על "מי אני" — והכותרת מתכווצת ונוסעת לפינה הימנית העליונה
4. הקו נמשך מתחתיה משמאל לימין

ואז ההצמדה משתחררת והסקשן נגלל רגיל. **זה מכוון:** כותרת נעוצה מעל טקסט שנוסע
מתחתיה — אין מיקום גלילה שבו הם לא חופפים, וכל תיקון (מסכות, פסים, אופסטים,
גובה עוטף נמדד) היה עוד דבר לתחזק. הטקסט עולה עם חשיפה בלוק־אחר־בלוק
(framer-motion `whileInView`, כמו `SectionHeading`), עם רווחים רחבים ביניהם.

### ContactStage

זום‑אאוט מפונן: הקליפ מתחיל במסך מלא, ומתרחק עד שהוא הופך למסך טלוויזיה
בתוך תמונת חדר תצוגה. הכל מורכב במרחב הפיקסלים של תמונת החדר
(`ROOM_W 6688 × ROOM_H 3764`), ורק scale ו‑offset אחד משתנים — זה זום אמיתי
של סצנה אחת, לא שני אלמנטים שמתקרבים.

### ארכיטקטורת המובייל

**החלטה אחת בכניסה לעמוד, לא שש החלטות בשישה סקשנים.**

`app/page.tsx` קורא את ה‑User-Agent **בשרת** ומעביר את הניחוש ל‑`HomeSwitch`,
שמפרסם אותו ב‑context ובוחר עץ: `MobileHome` או הסקשנים הדסקטופיים. אחרי
ההידרציה `matchMedia` מחליט באמת, אז ניחוש שגוי מתקן את עצמו.

בלי זה השרת שלח לכל טלפון עמוד דסקטופ שהוא זורק — כולל הפניה לקליפ 16:9
של 4MB שלא ייראה לעולם. **המחיר:** עמוד הבית עבר מ‑Static ל‑`ƒ (Dynamic)`
בפלט הבילד; שאר העמודים סטטיים. טאבלטים בדסקטופ — ה‑breakpoint הוא 768px.

---

## שיעורים שנקנו בדם — אל תלמד אותם מחדש

### וידאו

- **דפדפן חסר‑ראש לא מפענח H.264 בכלל.** הוא מציג רק את ה‑poster. כל אימות של
  תזמון וידאו חייב להיעשות על מכשיר אמיתי, על ידי המשתמש. אפשר לאמת גאומטריה
  חשבונית — לא תחושה.
- **GOP חייב להיות 5.** `servicesbg.mp4` נגלל עם `currentTime` בכל טיק. GOP
  ברירת מחדל (81 פריימים) מאלץ פענוח של עשרות פריימים בכל קפיצה והורס את
  האנימציה. בכל קידוד מחדש: `-g 5 -keyint_min 5 -sc_threshold 0`.
- **הגלילה מקוונטטת לרשת הפריימים** — seek רק כשהפריים באמת מתחלף, ולנקודת
  האמצע שלו. בלי זה כל פריים אנימציה שולח seek מיותר שעולה פענוח מלא.
- **תמיד להסיר אודיו** (`-an`).

### מובייל ו‑RTL

- **`ctx.filter` על קנבס לא עובד בספארי במובייל.** הלוגו `logo.png` הוא לבן על
  שקוף וחייב צביעה לשחור — עושים את זה עם composite `source-in`, לא עם filter.
- **`items-end` ב‑RTL הוא הצד השמאלי.** אותו דבר ל‑`justify-end` ו‑`flex-row-reverse`.
  זה גרם לשלושה באגים נפרדים.
- **גלישה אופקית ב‑RTL מזיזה את נקודת הציור של כל הדף** — לא רק מוסיפה סרגל.
  זו הייתה הרצועה הכהה בצד של כל סקשן. `SwipeCarousel` חותכת את עצמה ב‑`overflow-x: clip`.
- **`clip` ולא `hidden`** — `hidden` הופך אלמנט למיכל גלילה ומשבית `position: sticky`
  בתוכו, והאתר תלוי בהצמדות.
- **`100dvh` משתנה בגלילה** (הסרגל של הדפדפן נעלם ומופיע), וכל שינוי גובה בנה
  מחדש את סימולציית הדיו — זה נראה כמו הבהוב לבן עם הסרטון מציץ מתחת.
  משתמשים ב‑`100svh`, שהוא קבוע.
- **הדפדפן מבטל את זרם ה‑pointer** ברגע שהוא מחליט שגרירה היא גלילה. לכן הדיו
  "הופיע ומת". במגע משתמשים ב‑`touchmove` (passive) — הוא לא מבוטל.
  `touch-action: none` עובד אבל **כולא** את הקורא בהירו שתופס מסך מלא. לא לחזור לשם.
- **`overscroll-behavior-y: contain` על `html` וגם על `body`** — ב‑iOS מחוות
  הרענון נדרכת מהאלמנט שבאמת גולל.

### גלילה וביצועים

- **מחלקים גלילה לפי כמה משתנה בתמונה, לא לפי זמן.** אירוע של חצי שנייה צריך
  מסך או שניים; חמש שניות שבהן כמעט כלום לא קורה צריכות פחות ממסך.
- **קריאה אחרי כתיבה מכריחה layout flush.** בתוך handler של גלילה זה קטלני —
  לרכז את כל ה`getBoundingClientRect`/`clientWidth` **לפני** כל כתיבת סגנון.
  שני reflow כפויים בכל אירוע, מעל הפענוח של seek, זה מה שגרם לסקראב לפגר.
- **כותרת נעוצה מעל טקסט שנוסע מתחתיה תמיד תתנגש בו.** אין מיקום גלילה שבו
  לא. או שהטקסט מקבל פס אטום להיעלם מתחתיו, או שהכותרת לא נעוצה.
- **לא להעמיד סקשנים במקום כדי להאריך אותם.** עמוד שבו כמה סקשנים עוצרים
  מרגיש תקוע. להאט, לא לעצור.

### CSS

- **`mix-blend-mode` מתערבב רק בתוך ה‑stacking context שלו.** `relative` + `z-index`
  פותח כזה, ואז הבלנד לא רואה את הקנבס ומתערבב עם שקיפות — הכל נעלם. עוטף
  התוכן בהירו נשאר ב‑`z-auto` בכוונה.
- **היפוך על ידי blending, לא על ידי ציור על הקנבס** — הקנבס מוגבל ל‑2x פיקסלים
  והטלפון רץ ב‑3x, אז טקסט מצויר יוצא רך ומוכפל. `mix-blend-mode: difference`
  עם מקור **לבן** מתרנדר שחור על דף לבן ולבן מעל דיו כהה. לכן הצבעים בקוד
  נראים הפוכים — זה מכוון.
- **רווחי JSX סביב `inline-block`.** שורה חדשה ב‑JSX הופכת לרווח, אז
  `{"מילה"}` + span + `{"?"}` על שורות נפרדות אוספים שני רווחים מיותרים —
  והגדלה של הכותרת מכפילה אותם למכתשים. ורווח שצריך להימחק עם מילה חייב
  לשבת **בתוך** האלמנט שמתכווץ.
- **`text-wrap: balance`** במקום שבירות שורה ידניות. שבירה ידנית תקפה רק ברוחב
  אחד.

---

## החלטות עיצוב שכבר התקבלו

- **בלי מספרים ובלי לוגואים של לקוחות.** הפרקטיקה צעירה; מספר קטן בטיפוגרפיה
  גדולה מכריז על החוסר במקום לסגור אותו. במקום זה שלוש אמירות שנכונות ואי אפשר
  לזייף: איש אחד מקצה לקצה, הכל מאפס, מדברים ישירות איתי.
- **"מעצב מגיל 15, מפתח מגיל 17"** — ספציפי, לא ניתן להעתקה, ולא חושף גיל.
  **אין לציין את הגיל (31).**
- **CTA אחד**, לא שניים במשקל זהה.
- **אפור אחיד** לריחוף על השורות 1‑4, לא צבע לכל שורה.

---

## מה פתוח

### מובייל

- **`MobileServices` בנוי** — סקראב על קליפ 9:16 משלו
  (`public/mobile/videos/servicesbg-mobile.mp4`, 720×1280, 458 פריימים,
  15.267 שניות, GOP 5). `object-contain` בקופסה שנפתרת ב‑CSS, בלי
  טרנספורמציות — הקליפ כבר ממוסגר לטלפון.
- **החיתוך המוביילי עדיין הישן — בלי תרשים.** הדסקטופ קיבל קליפ חדש שבו
  התרשים מצויר על הנייר; המובייל לא. **זה הדבר הבא:** ייצוא 9:16 חדש שתואם
  לקליפ הדסקטופי, ואז אותו טיפול — הסרת בלוק "מי אני" מהנייר ומיפוי מחדש.
- שאר הסקשנים במובייל עדיין בעיצוב הדסקטופ נדחס.

### כללי

- `/about` — דף אמיתי. הקישור בעמודי המשנה מצביע היום ל‑`/#about`, שקיים.
- `herobg.mp4` (5.3MB) ו‑`servicesbg.mp4` (4.0MB) יורדים בכניסה.
- **ffmpeg מותקן** (Gyan build, winget). המתכון להמרת ייצוא CapCut:
  `ffmpeg -i in.mp4 -c:v libx264 -crf 20 -preset slow -g 5 -keyint_min 5 -sc_threshold 0 -pix_fmt yuv420p -an -movflags +faststart out.mp4`
  ל‑CapCut אין שליטה ב‑GOP והוא מייצא ~114; בלי ההמרה הסקראב מקרטע.
  המאסטרים של CapCut ב‑`.gitignore` — רק הקבצים שהאתר מגיש נכנסים ל‑git.

---

## דרך עבודה

- **לצבור ולדחוף פעם אחת בסוף סבב.** כל push הוא דפלוימנט, וכל דפלוימנט שובר
  את הדף שפתוח אצל עומר (version skew — ה‑HTML בקאש מצביע על קבצי JS שנמחקו).
  קומיט הוא מקומי וזול; רק push עולה.
- **לא מתחילים משימה בלי אישור מפורש** — ראה `CLAUDE.md`.
- **היסטוריית git נוקתה** מ‑215MB ל‑24MB (2026-08-04). כל גרסה של סרטון שמועלית
  נשמרת לנצח — לא להעלות סרטונים בלי צורך.
